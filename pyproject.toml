[project]
name = "sistema-invitaciones"
version = "0.1.0"
description = "Sistema web para la gestión de invitaciones digitales con validación QR."
readme = "README.md"
requires-python = ">=3.9,<4.0"
dependencies = [
    "Django>=4.2,<4.3",
    "psycopg[binary]>=3.2,<4.0",
]

[tool.uv]
package = false
